# Rubric notes — PBL 01

Use [../../rubrics/student-rubric.md](../../rubrics/student-rubric.md).

**Unacceptable:** a `booking` table with no uniqueness on `(resource_id, slot)` or equivalent exclusion constraint; utilization as a hardcoded number in the report.
