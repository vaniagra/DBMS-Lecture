# Requirements (PBL 07)

One seat, one student, one session. Invigilator cannot cover two rooms in the same slot. Question set not assigned to the writer as candidate (if that rule exists — document).
